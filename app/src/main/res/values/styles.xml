<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorlogin</item>
        <item name="colorPrimaryDark">@color/colorlogin</item>
        <item name="colorAccent">@color/colorlogin</item>
    </style>

    <style name="AppTheme.TemaPopup">
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
        <item name="android:radius">50dp</item>
        <item name="android:backgroundTint">@color/colorTransparente</item>
    </style>

</resources>